import React, { Component } from 'react';
import CheckupList from '../containers/checkup-list';

export default class CheckupView extends Component {
    render() {
        return (
            <CheckupList/>
        );
    }
}